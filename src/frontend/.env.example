# For local development (`npm start`), copy to `.env` and set the backend URL.
# In production (Docker build) `.env.production` is used and `REACT_APP_API_BASE`
# is empty, so the frontend uses relative URLs and talks to the same host that
# serves the React bundle.
REACT_APP_API_BASE=http://localhost:8080
